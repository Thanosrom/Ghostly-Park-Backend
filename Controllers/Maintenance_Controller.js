/* eslint-disable no-unused-vars */
const express = require('express');

const Maintenance_Controller = {
  async maintenance(req, res) {
    res.status(400).json(false);
  },
};

module.exports = Maintenance_Controller;
